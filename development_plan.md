# Development plan


 |                     | **in Progress** | **TODO Backlog** | **Completed** |
 |---------------------|-----------------|------------------|---------------|
 | **infrastruture**   |   | - On Visual Studio, use the test runner to run specific tests<br>  |  - build on linux<br>- build on windows<br>- set up a lib folder and a test folder<br>- logger (GLOG to be quick, but it can be improved later)<br>- string tokenizer<br>- string splitter<br>- round_to() compare() mechanism<br>- thread local decimal_precision / coherent tolerance - github actions: run tests on merge<br>- test cases possible in `.wkt` files formats in `geompp_tests/res` folder<br>- Docker based dev environment: Linux image<br>- create workflow to update version of c++, python and c#<br>- write some sample code<br>- write interface for C#<br>- write some c# sample code<br>- write interface for Python<br>- write some python sample code<br>- Build on windows via command line (install cmake and g++ on windows, use PowerShell)<br>- Docker based dev enviroment: Windows image<br>- write c# smoke tests<br>- write python smoke tests<br>- test covg +70% for all<br>  |
 | **2D geometry**     | - Shape2D for return type of geometrical operations<br>- ::FromWkt(str)->Shape2D, ::ToWkt()->str parsing and serializing<br>  | - Triangle2D::intersects(line, ray, line_seg)<br>- Polygon2D, contains(p)<br>- Polygon2D::intersects(line, ray, line_seg, triangle, polygon)<br>- List<Point2D>::convex_hull()->polygon<br>- Line2D::overlaps(line), Line2D::overlap(line)<br>- Ray2D::overlaps(line, ray)<br>- LineSegment2D::overlaps(line, ray, line_seg)<br>- Polyline2D::overlaps(line, ray, line_seg, polyline)<br>- Triangle2D::overlaps(line, ray, line_seg, polyline, triangle) (overlap for triangle is "adjacency")<br>- Polygon2D::overlaps(line, ray, line_seg, polyline, triangle, polygon) (overlap for polygon is "adjacency")<br>- Triangle2D::adjacent(line, ray, line_seg, polyline, triangle) (same as overlap)<br>- Polygon2D::adjacent(line, ray, line_seg, polyline, triangle, polygon) (same as overlap)<br>- Mesh2D class (set of triangles)<br>- Polygon2D::triangulate()->mesh<br>- Mesh2D::polygonize()->polygon - Triangle2D::intersection(triangle)<br>- Polygon2D::intersection(triangle, polygon)<br> | - Point2D, Vector2D, Line2D<br>- Line2D::contains(p), intersects(line), intersection(line)<br>- Ray2D, contains(p), intersection(line), intersection(ray)<br>- LineSegment2D, contains(p), intersection(line), intersection(ray), intersection(line_seg)<br>- Line2D,Ray2D,LineSegment2D::distance(p)<br>- LineSegment2D::interpolate(%)->p, location(p)->%<br>- Polyline2D, contains(p), distance(p)<br>- RemoveCollinear points<br>- RemoveDuplicate points <br>- Polyline2D::Wkt<br>- Polyline2D::interpolate(%)->p, location(p)->%<br>- Polyline2D::intersects(line, ray, line_seg, polyline)<br>- Triangle2D, Triangle2D::contains(p)<br>- Triangle2D::Wkt<br>- Triangle2D::vertices, area, perimeter<br> |
 | **3D geomtry**      | - Shape3D for return type of geometrical operations<br>- ::FromFile(wkb)->Shape2D, ::ToFile()->wkb parsing and serializing <br>  | - LineSegment3D::interpolate(%)->p, LineSegment3D::location(p)->%<br>- Polyline3D, Polyline3D::contains(p), Polyline3D::distance(p)<br>- Polyline3D::interpolate(%)->p, Polyline3D::locartion(p)->%<br>- Polyline3D::intersects(line, ray, line_seg, polyline)<br>- Plane, signed_distance, distance, project_onto, project_into<br>- Triangle3D, Triangle3D::contains(p)<br>- Triangle3D::intersects(line, ray, line_seg, triangle)<br>- Polygon3D, Polygon3D::contains(p)<br>- Polygon3D::intersects(line, ray, line_seg, triangle, polygon)<br>- List<Point3D>::convex_hull()->polygon<br>- Line3D::overlaps(line), Line3D::overlap(line)<br>- Ray3D::overlaps(line, ray)<br>- LineSegment3D::overlaps(line, ray, line_seg)<br>- Polyline3D::overlaps(line, ray, line_seg, polyline)<br>- Triangle3D::overlaps(line, ray, line_seg, polyline, triangle) (overlap for triangle is "coplanar, intersecting in 2D")<br>- Polygon3D::overlaps(line, ray, line_seg, polyline, triangle, polygon) (overlap for polygon is "coplanar, intersecting in 2D")<br>- Triangle3D::adjacent(line, ray, line_seg, polyline, triangle) (one side in common, no intersection)<br>- Polygon3D::adjacent(line, ray, line_seg, polyline, triangle, polygon) (one or more sides in common, no intersection)<br>- Mesh3D class (set of triangles)<br>- Polygon3D::triangulate()->mesh<br>- Mesh3D::polygonize()->polygon<br>- Triangle3D::intersection(triangle)<br>- Polygon3D::intersection(triangle, polygon)<br>- Triangle3D::overlap(triangle)<br>- Polygon3D::overlap(triangle, polygon)  |  - Point3D, Vector3D, Line3D<br>- Line3D::contains(p), Line3D::intersects(line), Line3D::intersection(line)<br>- Ray3D, contains(p), intersection(line, ray)<br>- LineSegment3D, contains(p), ntersection(line, ray, line_seg)<br>- Line3D, Ray3D, LineSegment3D::distance(p)<br>  |
 | **graphics**        |    | - add keys to move around with the camera<br>- add an input box to type in a new geometry<br>- add a key to delete a geometry    |  - Set up a window to display some line segments in OpenGL<br>  |



